kind: Deployment   #can scale replicas and also has rolling update.
apiVersion: apps/v1
metadata:
  name: deploy
  namespace: nginx
  labels:
    app: nginx

spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      name: pod
      labels:
        app: nginx

    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          volumeMounts:
            - mountPath: /var/www/html
              name: vol1
      volumes:
        - name: vol1
          persistentVolumeClaim:
            claimName: local-pvc
